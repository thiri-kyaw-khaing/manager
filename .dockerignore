# Dependencies and build output — rebuilt inside the image.
node_modules
.next
out
build

# Local env / secrets — inject at runtime, never bake in.
.env
.env.local
.env*.local

# VCS and tooling.
.git
.gitignore
.github
README.md

# Logs and OS cruft.
*.log
npm-debug.log*
.DS_Store
